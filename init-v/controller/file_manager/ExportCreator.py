from model.Configuration import Configuration
from model.Session import Session
from controller.init_v_controll_logic.ExportOptions import ExportOptions


class ExportCreator:
    """
    method to save a specific set of graphs on the disk.

    :param output_path: string of the output
    :param session: Session object
    :param options: ExportOptions object, contains the parameters of the export operation.
    """
    def export(self, output_path: str, session: Session, options: ExportOptions):
        #TODO implement
        pass